package com.example.postapp.ui
import android.view.LayoutInflater
import android.view.ViewGroup
import com.example.postapp.R
class PostRvAdappter var postsList:List<post>,var postItemClickListener)
RecyclerView.Adapter<PostRvAdappter.PostsViewHolder>(){
    override fun onCreateViewHolder(parent: ViewGroup, viewType:Int):
            var itemView=
    val parent
    LayoutInflater.from(parent.context).inflate(R.layout)
}